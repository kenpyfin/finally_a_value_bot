# Copy to .env in this skill folder if the bot process does not already export it.
# Prefer setting TAVILY_API_KEY in the main bot configuration .env (same as web_search).
TAVILY_API_KEY=tvly-your-key-here
